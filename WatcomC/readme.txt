match.obj is a replacement for the longest_match built to use with the Watcom C compiler. To use it you should
force C compiler to use C calling convention for the longest_match function (by default compiler uses fastcall).
